import { FormatDimensionPipe } from './format-dimension.pipe';

describe('FormatDimensionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDimensionPipe();
    expect(pipe).toBeTruthy();
  });
});
