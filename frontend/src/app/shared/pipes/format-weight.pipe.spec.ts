import { FormatWeightPipe } from './format-weight.pipe';

describe('FormatWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
